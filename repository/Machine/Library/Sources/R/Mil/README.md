This folder `R/Ml` contains the *Machine Language* (short: *ML*) compiler.
The compiler produces an in-memory representation of *ML* programs which then are interpreted by *Machine Interpreter* (`R/Ip`).
