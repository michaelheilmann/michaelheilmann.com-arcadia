class MouseButtonMessage {
  variable m_action
  variable m_button

  constructor(variable action, variable button) {
    set m_action, action
    set m_button, button
  }

  method getAction() {
    return m_action
  }

  method getButton() {
    return m_button
  }
}
